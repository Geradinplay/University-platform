# Исправление: Автоматическое переподключение к серверу

## Проблема
Когда приложение загружается без доступа к серверу (сервер был отключен при загрузке страницы), а затем сервер становится доступным, данные на странице остаются пустыми и не загружаются автоматически.

## Решение
Реализован новый модуль `ConnectionManager` который обеспечивает:

1. **Проверку соединения при загрузке** - при инициализации DOMContentLoaded проверяется доступность сервера
2. **Автоматическое переподключение** - если сервер недоступен, приложение периодически пытается переподключиться
3. **Визуальный индикатор статуса** - пользователь видит текущее состояние соединения
4. **Отложенная инициализация** - когда сервер восстанавливается, приложение автоматически инициализируется

## Файлы, которые были изменены

### 1. `src/js/utils/connectionManager.js` (новый файл)
Основной модуль для управления подключением. Класс `ConnectionManager` обеспечивает:

- **`checkConnection()`** - проверка доступности сервера через GET запрос к `/api/faculties`
- **`initialize(initFunction)`** - инициализация приложения с проверкой соединения
- **`setupReconnectionCheck(initFunction)`** - настройка процесса переподключения
- **`updateStatusIndicator(status)`** - обновление визуального индикатора на странице

Интервалы переподключения (в мс):
```javascript
[1000, 2000, 5000, 10000, 30000]  // Прогрессивное увеличение
```

Максимум попыток: 5

### 2. `src/js/main.js` (изменен)
**Импорт:**
```javascript
import { connectionManager } from './utils/connectionManager.js';
```

**Изменения в DOMContentLoaded:**
- Вместо прямой инициализации, используется `connectionManager.initialize(initializeApp)`
- Функция `initializeApp()` содержит всю логику инициализации
- При критической ошибке настраивается проверка переподключения

### 3. `public/index.html` (изменен)
Добавлен визуальный индикатор соединения в `admin-header`:
```html
<div id="connection-status" class="connection-indicator" title="Статус соединения с сервером">
  <span class="connection-dot disconnected"></span>
  <span class="connection-text">Подключение...</span>
</div>
```

### 4. `public/style.css` (изменен)
Добавлены стили для индикатора:
- `.connection-indicator` - контейнер индикатора
- `.connection-dot` - цветная точка индикатора
- Анимации `pulse-connected` и `pulse-disconnected`
- Классы для статусов: `.connected`, `.disconnected`, `.waiting`

## Как это работает

### Сценарий 1: Сервер доступен при загрузке
1. DOMContentLoaded вызывает `connectionManager.initialize()`
2. ConnectionManager проверяет соединение - успешно ✅
3. Индикатор меняется на "Подключено" (зеленый)
4. Выполняется `initializeApp()` - загружаются все данные
5. Приложение работает в нормальном режиме

### Сценарий 2: Сервер недоступен при загрузке
1. DOMContentLoaded вызывает `connectionManager.initialize()`
2. ConnectionManager проверяет соединение - ошибка ❌
3. Индикатор меняется на "Нет подключения" (красный)
4. Запускается `setupReconnectionCheck()` с прогрессивными интервалами:
   - 1-я попытка: через 1 сек
   - 2-я попытка: через 2 сек
   - 3-я попытка: через 5 сек
   - 4-я попытка: через 10 сек
   - 5-я попытка: через 30 сек

### Сценарий 3: Сервер восстанавливается после падения
1. Одна из попыток переподключения успешна ✅
2. Индикатор меняется на "Подключено" (зеленый)
3. Если приложение еще не инициализировано - выполняется `initializeApp()`
4. Если приложение уже инициализировано - выполняются callbacks переподключения

## Визуальный индикатор

**Статусы индикатора:**

| Статус | Цвет | Текст | Анимация |
|--------|------|-------|----------|
| Подключение | Белый | "Подключение..." | Мигание |
| Подключено | Зелёный | "Подключено" | Пульсирование |
| Нет подключения | Красный | "Нет подключения" | Мигание |
| Переподключение | Красный | "Переподключение... (N/5)" | Мигание |

Индикатор расположен в верхнем заголовке рядом с кнопкой "Выйти".

## Логирование

ConnectionManager отправляет подробные логи в консоль:
```
🔍 ConnectionManager: Проверяю соединение с сервером...
⚠️ Сервер недоступен. Жду восстановления подключения...
⏳ ConnectionManager: Попытка 1/5 через 1000ms...
✅ Сервер восстановлен! Переинициализирую приложение...
✅ Приложение успешно инициализировано
```

## Преимущества решения

1. **Надежность** - приложение работает даже если сервер был недоступен при загрузке
2. **Прозрачность** - пользователь видит текущее состояние соединения
3. **Автоматизм** - не требуется ручная перезагрузка страницы
4. **Масштабируемость** - можно добавить дополнительные callbacks для переподключения
5. **Безопасность** - проверка соединения не требует аутентификации

## Возможные расширения

### Добавление обработчика переподключения
```javascript
connectionManager.onReconnection(() => {
    console.log('Сервер восстановлен! Обновляю данные...');
    window.loadSchedule(); // Перезагрузить расписание
});
```

### Проверка готовности приложения
```javascript
if (connectionManager.isReady()) {
    // Приложение полностью инициализировано
}
```

### Ручное переподключение
```javascript
await connectionManager.reconnect(initializeApp);
```

## Тестирование

Для тестирования этого функционала:

1. **Остановить сервер** (если он запущен)
2. **Открыть приложение в браузере** - индикатор будет показывать "Нет подключения"
3. **Запустить сервер** - индикатор автоматически изменится на "Подключено"
4. **Проверить что данные загрузились** - расписание, преподаватели и т.д. должны отобразиться

Количество попыток и интервалы между ними можно настроить в начале файла `connectionManager.js`.

