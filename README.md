# University Platform - Система управления расписанием

## 📋 Описание
Веб-приложение для управления университетским расписанием с функциями авторизации, создания занятий, управления аудиториями, преподавателями и предметами.

## 🚀 Возможности

### Авторизация
- **Вход**: Авторизация существующего пользователя
- **Регистрация**: Создание нового аккаунта
- Автоматическое перенаправление на главную страницу после успешной авторизации
- JWT токен для безопасной аутентификации

### Управление расписанием
- Создание занятий с указанием предмета, преподавателя, аудитории и времени
- Drag & Drop для перемещения занятий между днями недели
- Буфер для временного хранения занятий
- Автоматические и ручные перерывы
- Проверка на пересечения во времени

### Управление данными
- Добавление/удаление **аудиторий**
- Добавление/удаление **преподавателей**
- Добавление/удаление **предметов**
- Просмотр списков всех сущностей

## 🛠 Установка и запуск

### Предварительные требования
- Node.js (версия 14 или выше)
- Backend API сервер (должен работать на `http://localhost:8080`)

### Установка зависимостей
```bash
npm install
```

### Запуск простого веб-сервера

#### Вариант 1: http-server (рекомендуется)
```bash
npm install -g http-server
http-server public -p 3000
```

#### Вариант 2: live-server
```bash
npm install -g live-server
live-server public --port=3000
```

#### Вариант 3: Python (если установлен)
```bash
cd public
python -m http.server 3000
```

После запуска откройте браузер и перейдите по адресу: `http://localhost:3000`

## 📁 Структура проекта

```
University-platform-1/
├── api/
│   ├── api.js              # API клиент для взаимодействия с backend
│   └── mockApi.js          # Mock API для тестирования
├── public/
│   ├── auth.html           # Страница авторизации/регистрации
│   ├── auth.css            # Стили для страницы авторизации
│   ├── index.html          # Главная страница приложения
│   └── style.css           # Основные стили
├── src/
│   ├── js/
│   │   ├── main.js         # Главный JavaScript файл
│   │   ├── auth/
│   │   │   └── LoginRegisterPage.js  # Модуль аутентификации
│   │   ├── components/
│   │   │   └── TaskManager.js
│   │   ├── handlers/
│   │   │   ├── contextMenuHandler.js
│   │   │   ├── dragDropHandler.js
│   │   │   ├── lessonFormHandler.js
│   │   │   └── connection-modal.js
│   │   ├── models/
│   │   │   └── Card.js
│   │   └── utils/
│   │       ├── collisionDetector.js
│   │       ├── selectPopulator.js
│   │       └── utils.js
│   └── css/
│       └── components/        # Модульные стили
└── package.json
```

## 🔐 API Endpoints

Приложение работает с следующими API endpoints:

### Авторизация
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/register` - Регистрация нового пользователя

### Расписание
- `GET /api/schedule` - Получение всего расписания
- `POST /api/schedule` - Создание нового занятия
- `PUT /api/schedule/:id` - Обновление занятия
- `DELETE /api/schedule/:id` - Удаление занятия

### Преподаватели
- `GET /api/professors` - Список всех преподавателей
- `POST /api/professors` - Создание преподавателя
- `DELETE /api/professors/:id` - Удаление преподавателя

### Аудитории
- `GET /api/classrooms` - Список всех аудиторий
- `POST /api/classrooms` - Создание аудитории
- `DELETE /api/classrooms/:id` - Удаление аудитории

### Предметы
- `GET /api/subjects` - Список всех предметов
- `POST /api/subjects` - Создание предмета
- `DELETE /api/subjects/:id` - Удаление предмета

### Перерывы
- `GET /api/breaks` - Список всех перерывов
- `POST /api/breaks` - Создание перерыва

## 🎨 Основные функции

### Страница авторизации (auth.html)
1. Переключение между формами входа и регистрации
2. Валидация полей формы
3. Отображение ошибок
4. Автоматическое сохранение JWT токена
5. Перенаправление после успешной авторизации

### Главная страница (index.html)
1. **Создание занятия**:
   - Выбор предмета, преподавателя, аудитории
   - Указание времени (формат ЧЧ:ММ-ЧЧ:ММ)
   - Добавление в буфер или на расписание

2. **Создание перерыва**:
   - Выбор дня недели
   - Указание времени начала и конца

3. **Управление сущностями**:
   - Добавление новых аудиторий, преподавателей, предметов
   - Просмотр списков существующих
   - Удаление через контекстное меню

4. **Drag & Drop**:
   - Перемещение занятий между днями
   - Перемещение в буфер и обратно
   - Визуальная обратная связь при перетаскивании

5. **Настройки**:
   - Автоматические перерывы (вкл/выкл)
   - Длительность перерыва
   - Разрешение пересечений во времени

## 💡 Использование

### Первый запуск
1. Откройте `http://localhost:3000/auth.html`
2. Зарегистрируйтесь или войдите в систему
3. После успешной авторизации вы будете перенаправлены на главную страницу

### Создание расписания
1. Добавьте необходимые предметы, преподавателей и аудитории
2. На вкладке "Создать занятие" заполните форму
3. Нажмите "Добавить занятие" - занятие появится в буфере
4. Перетащите занятие на нужный день недели
5. При необходимости добавьте перерывы

### Выход из системы
Нажмите кнопку "Выйти" в правом верхнем углу

## 🔧 Настройка

### Изменение URL API
Отредактируйте файл `api/api.js`:
```javascript
const API_BASE_URL = 'http://localhost:8080'; // Измените на ваш URL
```

### Настройка портов
По умолчанию frontend работает на порту 3000, backend - на 8080. Измените при необходимости.

## 🐛 Отладка

### Проверка консоли браузера
Откройте Developer Tools (F12) и проверьте:
- Наличие ошибок в консоли
- Network запросы к API
- LocalStorage (должен содержать 'jwt' после авторизации)

### Типичные проблемы
1. **"Ошибка соединения с сервером"** - проверьте, что backend запущен на порту 8080
2. **Перенаправление на auth.html** - JWT токен отсутствует или истёк
3. **Пустые выпадающие списки** - проблема с загрузкой данных с backend

## 📝 Дополнительная информация

### Хранение данных
- JWT токен хранится в `localStorage` с ключом `jwt`
- Имя пользователя хранится в `localStorage` с ключом `username`

### Безопасность
- Все API запросы автоматически включают JWT токен в заголовок `Authorization`
- При ошибке 401 (Unauthorized) пользователь перенаправляется на страницу авторизации

## 📄 Лицензия
UNLICENSED

---
**Дата создания**: 2026-02-09

